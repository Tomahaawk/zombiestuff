import React from 'react';
//import SurvivorInfoPanel from '../src/components/SurvivorInfoPanel';

const survivor = {name: '', age: '', infected: '', lonlat: 'POINT (0 0)'};


describe('<SurvivorInfoPanel />', () => {
  //const wrapper = shallow(<SurvivorInfoPanel singleSurvivor={survivor}/>);
  //Does not recognize "function = () => {}", why?
  it('TODO', () => {
    expect(true).to.equal(true);
  });
});
